import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
type C={id:string;classification:string;confidence:number;operator_id:string;model_version:string;captured_at:string;latitude?:number;longitude?:number};
function App(){const[c,setC]=useState<C[]>([]);useEffect(()=>{fetch('http://localhost:8000/api/v1/cases').then(r=>r.json()).then(setC).catch(()=>{})},[]);return <main><h1>Field Testing Operations</h1><p>{c.length} cases</p><table><thead><tr><th>Case</th><th>Result</th><th>Confidence</th><th>Model</th><th>Operator</th><th>Captured</th></tr></thead><tbody>{c.map(x=><tr key={x.id}><td>{x.id}</td><td>{x.classification}</td><td>{(x.confidence*100).toFixed(1)}%</td><td>{x.model_version}</td><td>{x.operator_id}</td><td>{new Date(x.captured_at).toLocaleString()}</td></tr>)}</tbody></table></main>}createRoot(document.getElementById('root')!).render(<App/>);
