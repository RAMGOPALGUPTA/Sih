import * as SQLite from'expo-sqlite';import*as FileSystem from'expo-file-system';
class StorageService{db=null;async init(){this.db=await SQLite.openDatabaseAsync('FieldDrugTesting.db');await this.db.execAsync(`CREATE TABLE IF NOT EXISTS officers(id INTEGER PRIMARY KEY AUTOINCREMENT,officer_id TEXT UNIQUE NOT NULL,name TEXT NOT NULL,phone TEXT UNIQUE NOT NULL,pin TEXT NOT NULL,designation TEXT,district TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP);CREATE TABLE IF NOT EXISTS test_records(id INTEGER PRIMARY KEY AUTOINCREMENT,officer_id TEXT NOT NULL,test_type TEXT,result TEXT,confidence_score REAL,image_path TEXT,image_hash TEXT,latitude REAL,longitude REAL,accuracy REAL,device_info TEXT,timestamp TEXT DEFAULT CURRENT_TIMESTAMP,synced INTEGER DEFAULT 0,digital_signature TEXT);CREATE TABLE IF NOT EXISTS metadata(id INTEGER PRIMARY KEY AUTOINCREMENT,test_record_id INTEGER NOT NULL,gps_latitude REAL,gps_longitude REAL,accuracy_meters REAL,device_info TEXT,app_version TEXT,network_type TEXT);CREATE TABLE IF NOT EXISTS sync_queue(id INTEGER PRIMARY KEY AUTOINCREMENT,test_record_id INTEGER NOT NULL UNIQUE,status TEXT DEFAULT 'pending',error_message TEXT,retry_count INTEGER DEFAULT 0,created_at TEXT DEFAULT CURRENT_TIMESTAMP);`);const dir=FileSystem.documentDirectory+'images/';const info=await FileSystem.getInfoAsync(dir);if(!info.exists)await FileSystem.makeDirectoryAsync(dir,{intermediates:true})}
async addOfficer(o){return(await this.db.runAsync('INSERT OR REPLACE INTO officers(officer_id,name,phone,pin,designation,district) VALUES(?,?,?,?,?,?)',[o.officer_id,o.name,o.phone,o.pin,o.designation||'',o.district||''])).lastInsertRowId}async getOfficer(id){return this.db.getFirstAsync('SELECT * FROM officers WHERE officer_id=?',[id])}async addTestRecord(t){const r=await this.db.runAsync('INSERT INTO test_records(officer_id,test_type,result,confidence_score,image_path,image_hash,latitude,longitude,accuracy,device_info,digital_signature) VALUES(?,?,?,?,?,?,?,?,?,?,?)',[t.officer_id,t.test_type,t.result,t.confidence_score,t.image_path,t.image_hash,t.latitude,t.longitude,t.accuracy,t.device_info,t.digital_signature]);await this.db.runAsync('INSERT OR REPLACE INTO sync_queue(test_record_id,status) VALUES(?,?)',[r.lastInsertRowId,'pending']);return r.lastInsertRowId}async getTestRecords(id){return this.db.getAllAsync('SELECT * FROM test_records WHERE officer_id=? ORDER BY timestamp DESC',[id])}async getStats(id){return this.db.getFirstAsync(`SELECT COUNT(*) total_tests,SUM(result='positive') positive_count,SUM(result='negative') negative_count,SUM(result='inconclusive') inconclusive_count,COALESCE(AVG(confidence_score),0) avg_confidence FROM test_records WHERE officer_id=?`,[id])}async saveImage(uri){const dest=FileSystem.documentDirectory+'images/'+Date.now()+'.jpg';await FileSystem.copyAsync({from:uri,to:dest});return dest}}
export default new StorageService();
