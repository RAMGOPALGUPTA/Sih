import*as Location from'expo-location';class LocationService{async getCurrentLocation(){const p=await Location.requestForegroundPermissionsAsync();if(p.status!=='granted')return null;const x=await Location.getCurrentPositionAsync({accuracy:Location.Accuracy.Balanced});return{latitude:x.coords.latitude,longitude:x.coords.longitude,accuracy:x.coords.accuracy}}}export default new LocationService();
